# Geo-Fail2Ban Firewall Whitelist (TEMPLATE)
#
# Copy this file to whitelist.txt and add your trusted IPs (one per line).
# whitelist.txt is git-ignored so your REAL IPs are never committed/pushed.
#
#   cp config/whitelist.txt.example config/whitelist.txt
#   sudo nano config/whitelist.txt
#
# The IPs below are RFC 5737 documentation placeholders — replace them.
#
# Example:
# 203.0.113.10
# 203.0.113.20
# 203.0.113.30
# 203.0.113.31
#
# To apply: sudo bash scripts/setup-firewall.sh

# Add your IPs below:
