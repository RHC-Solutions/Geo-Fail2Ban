PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
# Refresh AbuseIPDB blacklist daily (free API allows only 5 blacklist requests/day, quota resets 00:00 UTC).
# Runs shortly after the reset so this server gets its request before other hosts sharing the key.
# Add-only ipset: IPs are blocked permanently.
5 1 * * * root /opt/geo-fail2ban/abuseipdb_blocker.py >> /var/log/fail2ban-abuseipdb.log 2>&1
